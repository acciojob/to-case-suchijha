function toCase(str) {
  let lowerCaseStr = str.toLowerCase();
let upperCaseStr = str.toUpperCase();
   
let output = lowerCaseStr + "-" + upperCaseStr;

return output;
}

// DO not change the code below

//const text = prompt("Enter text:");
//alert(toCase(text));
